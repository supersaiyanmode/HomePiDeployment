
 - hosts: all
   become: yes
   become_user: root
   become_method: su
   gather_facts: no
   pre_tasks:
       - name: 'Install python'
         raw: pacman --noconfirm -S python

   roles:
       - role: install_boot
       - role: install_basic_packages
       - role: install_user
       - role: create_directory_structure
       - role: install_updater
       - role: clone_repos
       - role: install_piserver
       - role: install_chromium_service

